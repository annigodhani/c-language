#include<stdio.h>
int main() {
  
   char a[20],b[20];
   int k,i;
   printf("Enter FirstName :");
   gets(a);
   printf("Enter LastName :");
   gets(b);
   printf("your Fullname is :%s %s",a,b);
   for(k=0;k<i;k++) {
 }
}